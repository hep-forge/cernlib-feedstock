# conda_build_config.yaml

# Specify the custom channels to use
channels:
  - hep-forge  # Your custom channel
  - conda-forge        # Additional channel (optional)
  - defaults           # Default channel (optional)
